import { defineConfig } from "vite";

// IMPORTANT: set base to your repo name when deploying to GitHub Pages.
// Example repo: https://github.com/<you>/quantum-dungeon  -> base: "/quantum-dungeon/"
export default defineConfig({
  base: "./", // keep "./" for easiest local + Pages; we can switch later if needed
});